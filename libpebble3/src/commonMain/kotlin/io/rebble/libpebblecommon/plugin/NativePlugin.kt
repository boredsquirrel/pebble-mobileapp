package io.rebble.libpebblecommon.plugin

/**
 * A built-in plugin the app ships (a phone-side data source like Music or Weather), shown in the
 * Plugins tab's "Built-in Plugins" section.
 */
interface NativePlugin : Plugin
